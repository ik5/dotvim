" ==================================================
" viewdoc uses splits, not tabs
" ==================================================

" do not map K
let g:no_viewdoc_maps=1
" where to open the window
let g:viewdoc_open='belowright new'
" don't display empty doc window
let g:viewdoc_openempty=0
" don't switch to the doc window when it opens
let g:viewdoc_dontswitch=0
